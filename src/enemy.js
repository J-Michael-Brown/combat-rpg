export class Enemy {
  constructor(title, power, credits, creditsMessage, attacks) {
    this.title = title;
    this.power = power;
    this.credits = credits;
    this.creditsMessage = creditsMessage;
    this.attacks = attacks;
  }
}
