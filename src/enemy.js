export class Enemy {
  constructor(title, power, credits, creditsMessage) {
    this.title = title;
    this.power = power;
    this.credits = credits;
    this.creditsMessage = creditsMessage;
  }
}
